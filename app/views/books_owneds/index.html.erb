<p id="notice"><%= notice %></p>

<h1>Books Owneds</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Book</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books_owneds.each do |books_owned| %>
      <tr>
        <td><%= books_owned.user_id %></td>
        <td><%= books_owned.book_id %></td>
        <td><%= link_to 'Show', books_owned %></td>
        <td><%= link_to 'Edit', edit_books_owned_path(books_owned) %></td>
        <td><%= link_to 'Delete', delete_books_owned_path(books_owned) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Books Owned', new_books_owned_path %>
<%= link_to 'Books', books_path %>
<%= link_to 'Users', users_path %>
